---
import type { ShivaniBook } from '../../types/book';

interface Props {
  subject: ShivaniBook;
}

const { subject } = Astro.props;
---

<div class="bg-white rounded-xl shadow-sm p-6">
  <h3 class="text-xl font-semibold text-primary mb-2">{subject.name}</h3>
  <p class="text-sm text-slate-500 mb-4">Subject Code: {subject.code}</p>
  
  <div class="space-y-4">
    <div class="flex items-center justify-between bg-slate-50 p-4 rounded-lg">
      <div class="flex items-center">
        <span class="text-2xl mr-3">📚</span>
        <div>
          <p class="font-medium text-slate-700">Shivani Publication Book</p>
          <p class="text-sm text-slate-500">Latest Edition</p>
        </div>
      </div>
      <a 
        href={subject.downloadUrl} 
        class="bg-accent text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-accent-dark transition-colors"
      >
        Download
      </a>
    </div>
  </div>
</div>